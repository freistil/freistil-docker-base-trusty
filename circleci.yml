machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - rake build

test:
  override:
    - rake test

deployment:
  hub: 
    branch: master
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - rake push
